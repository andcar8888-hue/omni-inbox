# Base URL of the Omni-Inbox backend API (CodeIgniter 4, versioned under /api/v1).
# For local development the backend runs on `php spark serve` (port 8080).
# Copy this file to `.env` (or `.env.local`) and adjust if your backend runs elsewhere.
VITE_API_BASE_URL=http://localhost:8080/api/v1
