#--------------------------------------------------------------------
# Omni-Inbox backend environment configuration
#
# Copy this file to `.env` and fill in real values for local development.
# `.env` is gitignored — never commit real credentials.
#--------------------------------------------------------------------

#--------------------------------------------------------------------
# ENVIRONMENT
#--------------------------------------------------------------------

CI_ENVIRONMENT = development

#--------------------------------------------------------------------
# APP
#--------------------------------------------------------------------

app.baseURL = 'http://localhost:8080/'

#--------------------------------------------------------------------
# DATABASE (MySQL 8, InnoDB, utf8mb4 — see docs/db-schema.sql)
#--------------------------------------------------------------------

database.default.hostname = localhost
database.default.database = omni_inbox
database.default.username = root
database.default.password =
database.default.DBDriver = MySQLi
database.default.DBPrefix =
database.default.port = 3306
database.default.charset = utf8mb4
database.default.DBCollat = utf8mb4_general_ci

database.tests.hostname = localhost
database.tests.database = omni_inbox_test
database.tests.username = root
database.tests.password =
database.tests.DBDriver = MySQLi
database.tests.DBPrefix =
database.tests.charset = utf8mb4
database.tests.DBCollat = utf8mb4_general_ci
database.tests.port = 3306

#--------------------------------------------------------------------
# ENCRYPTION
#--------------------------------------------------------------------

encryption.key =

#--------------------------------------------------------------------
# AUTH (JWT for the API — see CLAUDE.md)
#--------------------------------------------------------------------

# JWT_SECRET =

#--------------------------------------------------------------------
# TELEGRAM (Bot API channel — see backend/app/Services/Channels/TelegramChannel.php)
#--------------------------------------------------------------------

# Bot token from @BotFather, e.g. 123456789:AAExampleTokenStringHere.
# TelegramChannel uses this for outbound sendMessage calls. The literal
# placeholder below is treated as "not configured" (sends are marked failed).
TELEGRAM_BOT_TOKEN = REPLACE_WITH_REAL_BOT_TOKEN

# The secret_token you register via Telegram's setWebhook. Telegram echoes it on
# every delivery in the X-Telegram-Bot-Api-Secret-Token header; the webhook
# controller compares it constant-time and 401s on mismatch. The literal
# placeholder below is treated as "not configured" (the webhook fails closed and
# rejects every request until a real secret is set).
TELEGRAM_WEBHOOK_SECRET = REPLACE_WITH_REAL_WEBHOOK_SECRET

#--------------------------------------------------------------------
# SESSION
#--------------------------------------------------------------------

# session.driver = 'CodeIgniter\Session\Handlers\FileHandler'
# session.savePath = null

#--------------------------------------------------------------------
# LOGGER
#--------------------------------------------------------------------

# logger.threshold = 4
